{
  "name": "@imagent/studio",
  "version": "0.2.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "Imagent Electron desktop application",
  "main": "dist/main/main.mjs",
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names main,preload,renderer,electron --prefix-colors yellow,cyan,green,magenta \"vite build --watch --config vite.main.config.ts\" \"vite build --watch --config vite.preload.config.ts\" \"vite --config vite.renderer.config.ts\" \"wait-on dist/main/main.mjs && electron .\"",
    "build": "vite build --config vite.main.config.ts && vite build --config vite.preload.config.ts && vite build --config vite.renderer.config.ts",
    "build:main": "vite build --config vite.main.config.ts",
    "build:preload": "vite build --config vite.preload.config.ts",
    "build:renderer": "vite build --config vite.renderer.config.ts",
    "start": "electron .",
    "package": "bun run package:win",
    "package:win": "bun run rebuild && bun run build && electron-builder --win nsis --publish never",
    "package:mac": "bun run rebuild && bun run build && electron-builder --mac dmg --publish never",
    "package:linux": "bun run rebuild && bun run build && electron-builder --linux AppImage --publish never",
    "typecheck": "tsc --noEmit",
    "rebuild": "electron-rebuild -f -w better-sqlite3 -w sharp",
    "postinstall": "echo 'Run `bun run --filter @imagent/studio rebuild` before launching the desktop app to rebuild native modules for Electron'",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "dependencies": {
    "@dnd-kit/core": "catalog:",
    "@imagent/config": "workspace:*",
    "@imagent/core": "workspace:*",
    "@imagent/ipc": "workspace:*",
    "@imagent/persistence": "workspace:*",
    "@imagent/providers": "workspace:*",
    "@imagent/ui": "workspace:*",
    "@phosphor-icons/react": "catalog:",
    "@radix-ui/react-dialog": "catalog:",
    "@radix-ui/react-dropdown-menu": "catalog:",
    "better-sqlite3": "catalog:",
    "events": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "sharp": "catalog:",
    "zustand": "catalog:"
  },
  "devDependencies": {
    "@electron/rebuild": "catalog:",
    "@tailwindcss/vite": "catalog:",
    "@types/better-sqlite3": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "concurrently": "catalog:",
    "electron": "catalog:",
    "electron-builder": "catalog:",
    "tailwindcss": "catalog:",
    "vite": "catalog:",
    "wait-on": "catalog:"
  }
}
